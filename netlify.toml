[build]
  publish = "build"
  command = "bash prebuild.sh && npm run build"
  environment = { 
    NODE_ENV = "production"
  }

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
