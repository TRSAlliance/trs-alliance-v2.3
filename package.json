{
  "name": "trs-alliance-v2.3",
  "version": "2.3.8",
  "description": "TRS CLI and Sentinel deployment logic",
  "scripts": {
    "prebuild": "node setup-env.js",
    "build": "npm run prebuild && next build",
    "trs-build": "npm run prebuild && next build",
    "build:clean": "npm run prebuild && rm -rf .next out && next build",
    "build:safe": "npm config set optional false && npm install && netlify build",
    "dev": "next dev",
    "start": "next start",
    "lint": "next lint",
    "netlify:build": "npm run build",
    "netlify:dev": "netlify dev"
  },
  "dependencies": {
    "next": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  },
  "license": "MIT",
  "author": "TRSAlliance"
}
